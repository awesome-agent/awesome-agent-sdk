{
  "name": "awesome-agent-sdk",
  "private": true,
  "description": "Agentic AI SDK — core engine + provider adapters",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
